{
  "name": "json-to-dom",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "workspaces": [
    "dist"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "package": "npm pack --workspace dist",
    "publish-package": "npm publish --workspace dist"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
