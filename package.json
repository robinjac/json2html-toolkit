{
  "name": "json-to-dom",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "dist"
  ],
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "package": "npm pack --workspace dist"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
